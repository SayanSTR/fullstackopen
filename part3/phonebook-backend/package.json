{
  "name": "phoneboook-backend",
  "version": "0.0.1",
  "description": "Backend for the Phonebook app",
  "license": "MIT",
  "author": "SayanSTR",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd ../phonebook-frontend/ && npm run build && cp -r dist ../phonebook-backend",
    "deploy": "fly deploy",
    "deploy:dev": "npm run build:ui && npm run dev",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@flydotio/dockerfile": "^0.7.10",
    "@stylistic/eslint-plugin": "^5.2.1",
    "eslint": "^9.31.0",
    "globals": "^16.3.0"
  }
}
